{
  "name": "tron-sr-monitor",
  "version": "1.0.0",
  "main": "index.html",
  "scripts": {
    "start": "node -e \"const express=require('express');const app=express();app.use(express.static(__dirname));app.listen(process.env.PORT||3000)\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "axios": "^1.6.0"
  }
}
